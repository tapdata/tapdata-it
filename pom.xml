<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>io.tapdata</groupId>
    <artifactId>tapdata-it</artifactId>
    <packaging>pom</packaging>
    <name>TapData Integration Testing</name>
    <url>https://tapdata.io</url>

    <version>1.0-SNAPSHOT</version>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- PDK API 版本：按 2.0.5 编译（mysql-connector 产品线版本），
             仅调用 2.0.5 已有的 API，保证运行时兼容任意更高版本环境 -->
        <tapdata.pdk.api.version>2.0.5-SNAPSHOT</tapdata.pdk.api.version>
        <junit.version>5.9.2</junit.version>
        <jackson.version>2.18.3</jackson.version>
        <!-- DBForge 控制面 Java SDK：连接器 IT 经 DbForgeProvisioner 按需动态供应数据库（-Ddbforge.sdk.version= 可覆盖） -->
        <dbforge.sdk.version>0.6.13-SNAPSHOT</dbforge.sdk.version>

        <!-- L3 集成测试框架依赖版本 -->
        <!-- fabric8 Kubernetes 客户端：动态在 k3s 部署完整产品、查询/分配 NodePort（6.x 单 artifact DSL 稳定） -->
        <fabric8.version>6.10.0</fabric8.version>
        <!-- 管理端 SDK（TmAvailableRestTemplate / 认证 / VersionHeaderInterceptor），来自 tapdata/manager/tm-sdk -->
        <tm.sdk.version>1.0.4-SNAPSHOT</tm.sdk.version>
        <!-- 管理端数据传输对象（TaskDto / DAG / DatabaseNode / Status），来自 tapdata/manager/tm-common -->
        <tm.common.version>0.0.3-SNAPSHOT</tm.common.version>
    </properties>

    <!-- Nexus 公有云 maven-snapshots 仓库：tapdata-pdk-api、tapdata-api 等 SNAPSHOT
         制品发布于此，公网可达且无需认证；Release 依赖（jackson、junit 等）从 Central 默认解析 -->
    <repositories>
        <repository>
            <id>nexus-tapdata-net</id>
            <name>Nexus Snapshot Repository</name>
            <url>https://nexus.tapdata.net/repository/maven-snapshots/</url>
            <snapshots>
                <enabled>true</enabled>
                <updatePolicy>always</updatePolicy>
            </snapshots>
            <releases>
                <enabled>false</enabled>
            </releases>
        </repository>
    </repositories>

    <modules>
        <module>tapdata-it-common</module>
        <module>tapdata-connector-it</module>
        <!-- L3 完整产品集成测试框架：框架本体 + 端到端用例 + 稳定性专项 -->
        <module>product-it-core</module>
        <module>product-e2e</module>
        <module>product-stability</module>
    </modules>

    <distributionManagement>
        <repository>
            <id>nexus-tapdata-net</id>
            <name>Nexus Release Repository</name>
            <url>https://nexus.tapdata.net/repository/maven-public/</url>
        </repository>
        <snapshotRepository>
            <id>nexus-tapdata-net</id>
            <name>Nexus Snapshot Repository</name>
            <url>https://nexus.tapdata.net/repository/maven-snapshots/</url>
        </snapshotRepository>
    </distributionManagement>
</project>
